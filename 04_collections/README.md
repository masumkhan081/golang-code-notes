# 04_collections — arrays, slices, maps

This section covers Go's core collections with examples and edge cases:

- `arrays`: Fixed-length, value semantics
- `slices`: Dynamic, reference semantics
- `maps`: Key-value pairs, reference semantics

Each subfolder contains a runnable Go file for that topic.
