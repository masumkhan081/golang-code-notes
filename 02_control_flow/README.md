# 02_control_flow

This section covers Go's control flow constructs with examples and edge cases:

- `if`: Conditional branching
- `for`: Looping (the only loop keyword in Go)
- `switch`: Multi-way branching, value/type/condition-based
- `labels`: Advanced control flow with labels, break, continue, goto

Each subfolder contains a runnable Go file for that topic.
