# 11_generics

Type parameters (Go 1.18+).

## Contents

- `generics.go` — basic generic function and type
- `advanced/generics_advanced.go` — **constraints (`comparable`, `~T`, `cmp.Ordered`), type inference rules, generic types and methods, `slices`/`maps`/`cmp` stdlib tour, when NOT to use generics**

## Cross-references

- Interview Q&A: `../25_interview_questions/06_generics.md`
